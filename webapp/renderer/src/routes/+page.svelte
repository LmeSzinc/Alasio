<!-- Root route placeholder: the real route is driven by the shared state
     (useSharedState navigates to /setup, /loading, /app or /error on startup),
     so this page is only visible for one frame at most. -->
