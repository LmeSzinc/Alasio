# default mod entry
# key: mod name, value: path to mod root folder
DICT_MOD_ENTRY = {
    'alasio': {
        # relative path from running folder to mod root folder
        # default to ""
        'root': '',
        # relative path from mod root to gui.index.json
        # default to "module/config/gui.index.json"
        'gui_index_file': 'alasio/config_alasio/gui.index.json',
        # relative path from mod root to model.index.json
        # default to "module/config/model.index.json"
        'model_index_file': 'alasio/config_alasio/model.index.json',
    },
}
